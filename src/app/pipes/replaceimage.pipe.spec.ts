import { ReplaceimagePipe } from './replaceimage.pipe';

describe('ReplaceimagePipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceimagePipe();
    expect(pipe).toBeTruthy();
  });
});
